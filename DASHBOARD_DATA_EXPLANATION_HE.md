# הסבר נתוני הדאשבורד

המסמך הזה מסביר את הנתונים שמוצגים בדאשבורד של Polymarket BTC Collector. הדאשבורדים הניהוליים מחושבים מעל חמש הטבלאות הקיימות: `events`, `orderbook_log`, `btc_volume_log`, `rules`, `deals`.

## עקרונות חישוב

- מדדים פיננסיים מחושבים מעסקאות סגורות בלבד, כלומר עסקאות עם `result` של `win` או `loss` ועם `exit_price`.
- עסקאות פתוחות נספרות בנפרד, אבל לא משפיעות על רווח/הפסד, ROI, יחס רווח להפסד או drawdown.
- שדה `השקעה לעסקה` הוא פילטר תצוגה בלבד. הוא לא משנה את העסקאות ב־DB, אלא מחשב איך התוצאות היו נראות לפי סכום השקעה אחר.
- `טווח תאריכים` מסנן את התצוגה לפי זמן יציאה של עסקאות סגורות. בטווח מותאם מזינים `מתאריך ושעה` ו־`עד תאריך ושעה` בפורמט `DD/MM/YYYY HH:mm`, לפי שעון ישראל. בטבלאות הגולמיות עדיין מוצגים הנתונים האחרונים מה־DB.

## סקירה ניהולית

- `רווח/הפסד נטו`: סך הרווח או ההפסד מכל העסקאות הסגורות בטווח הנבחר.
- `עסקאות סגורות`: מספר העסקאות שהסתיימו ברווח או הפסד. לידו מוצג מספר העסקאות שעדיין פתוחות.
- `אחוז הצלחה`: אחוז העסקאות הרווחיות מתוך כלל העסקאות הסגורות.
- `ROI ממוצע לעסקה`: ממוצע התשואה באחוזים לכל עסקה סגורה.
- `יחס רווח להפסד`: הרווח הגולמי חלקי ההפסד הגולמי. ערך גבוה יותר מצביע על יחס טוב יותר בין רווחים להפסדים.
- `ירידה מקסימלית`: הירידה הגדולה ביותר מהשיא בעקומת הרווח המצטברת של העסקאות הסגורות.
- `החוק המוביל`: החוק שהניב את הרווח נטו הגבוה ביותר בטווח הנבחר.
- `תקינות נתונים`: אינדיקציה מהירה לבעיות בדגימות Orderbook.

## ביצועי חוקים

המקטע מציג ביצועים לכל חוק מסחר:

- כמות עסקאות סגורות ופתוחות.
- כמות עסקאות רווחיות והפסדיות.
- אחוז הצלחה.
- רווח/הפסד נטו.
- ROI ממוצע.
- יחס רווח להפסד.
- תוחלת לעסקה.
- ירידה מקסימלית לפי אותו חוק.
- מחירי כניסה, סטופ ויעד רווח.

המיון מתבצע לפי רווח/הפסד נטו בעסקאות סגורות.

## תמונת סיכון

- `הון מסכם`: סכום הרווח/הפסד המצטבר של העסקאות הסגורות.
- `שיא הון`: הנקודה הגבוהה ביותר בעקומת הרווח המצטברת.
- `ירידה מקסימלית`: הירידה הגדולה ביותר מהשיא.
- `רצף הפסדים`: רצף העסקאות ההפסדיות הארוך ביותר.
- `רווח ממוצע`: ממוצע הרווח בעסקאות שהסתיימו ברווח.
- `הפסד ממוצע`: ממוצע ההפסד בעסקאות שהסתיימו בהפסד.
- `העסקה הטובה ביותר`: העסקה עם הרווח הגבוה ביותר.
- `העסקה החלשה ביותר`: העסקה עם ההפסד הגבוה ביותר.
- `סיבות יציאה`: פירוק ביצועים לפי סיבת סגירת עסקה, למשל take profit, stop loss או resolution.

## תנאי שוק

בשלב הנוכחי המקטע משתמש בשדות אמינים מתוך טבלת `deals`:

- `ביצועים לפי צד`: השוואה בין עסקאות YES לבין עסקאות NO.
- `ביצועים לפי מחיר כניסה`: חלוקה לטווחי מחיר כניסה, כדי לזהות איפה החוקים עובדים טוב יותר.

בהמשך אפשר להרחיב את המקטע הזה לניתוח לפי spread, midpoint, trades count, volume ו־BTC volume delta מתוך `orderbook_log` ו־`btc_volume_log`.

## מגמות לאורך זמן

המקטע מקבץ עסקאות סגורות לפי תאריך מקומי ומציג לכל יום:

- מספר עסקאות סגורות.
- מספר רווחיות והפסדיות.
- אחוז הצלחה יומי.
- רווח/הפסד נטו יומי.
- ROI ממוצע יומי.
- עמודת בר שממחישה את עוצמת הרווח או ההפסד.

## גרפים

המקטע מציג גרפים בסיסיים מתוך הנתונים המקובצים:

- `רווח/הפסד יומי נטו`: ברים לפי רווח או הפסד בכל יום.
- `עסקאות סגורות ליום`: ברים לפי כמות העסקאות שנסגרו בכל יום.

הגרפים מציגים עד 30 ימים מקובצים מתוך הטווח שנבחר.

## איכות נתונים

המקטע בודק בעיות פנימיות אפשריות:

- עסקאות ללא חוק תואם.
- עסקאות ללא אירוע תואם.
- עסקאות פתוחות מעל 30 דקות.
- חוסר התאמה בין סטטוס אירוע לבין השדה `closed`.
- דגימות Orderbook שאינן במצב הצלחה.
- מחירי Orderbook מחוץ לטווח תקין של 0 עד 1.
- שגיאות בדגימות נפח BTC.
- דלתא שלילית בנפח BTC.

סטטוס `ok` אומר שלא נמצאו בעיות בבדיקות. סטטוס `needs_review` אומר שיש לפחות בדיקה אחת שדורשת בדיקה.

## בריאות מערכת

המקטע מציג תמונת מצב תפעולית:

- סטטוס מערכת כללי.
- גודל קובץ ה־DB.
- האירוע האחרון שנקלט.
- דגימת Orderbook אחרונה.
- דגימת נפח BTC אחרונה.
- מצב איסוף Coinbase.
- כמות עסקאות פתוחות וסך עסקאות.
- ספירת רשומות מרכזיות בכל אחת מהטבלאות.

## טבלאות גולמיות

בסוף המסך עדיין מוצגות הטבלאות הגולמיות:

- `events`
- `orderbook_log`
- `btc_volume_log`
- `rules`
- `deals`

כותרות הטבלאות האלה נשארות בשמות המקוריים כדי לשמור על התאמה ישירה לשמות העמודות ב־SQLite ולייצוא Excel.
